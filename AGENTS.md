# AGENTS.md

本项目用于将 Markdown 文档转换为 docx 格式，转换过程中的所有排版样式均由 YAML 配置文件驱动。

## 核心设计原则

**样式与内容分离**：YAML 配置文件是样式定义的唯一来源。其中每一个顶级键（Top-level key）均映射至 Word 内的一个具体样式（Style）。

**严禁直接格式化**：在生成 docx 的过程中，**绝对禁止**为段落或 Run 对象设置独立的局部格式（如直接指定字体、字号或颜色）。必须严格引用 YAML 中预定义的命名样式，确保文档结构的规范性与一致性。